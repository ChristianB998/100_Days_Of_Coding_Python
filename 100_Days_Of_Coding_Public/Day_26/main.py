# List Comprehension
#new_list = [new_item for item in list if test]

# with if statement
# name = ["Alex", "Beth", "Caroline", "Dave", "Eleanor", "Freddie"]
# should display the names with only four letters
# short_names = [name for name in names if len(name) < 5]